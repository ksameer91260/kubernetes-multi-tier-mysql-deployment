apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: prod-mysql-app
type: Opaque
stringData:
  mysql-root-password: SuperSecurePassword123
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-db
  namespace: prod-mysql-app
spec:
  selector:
    matchLabels:
      app: mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - image: mysql:5.7
        name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-root-password
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: prod-mysql-app
spec:
  ports:
  - port: 3306
  selector:
    app: mysql
  clusterIP: None
